{
  "name": "beauty-advisor-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start"
  },
  "dependencies": {},
  "devDependencies": {}
}
